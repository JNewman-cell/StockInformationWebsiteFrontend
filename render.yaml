services:
  - type: web
    name: stockinformationwebsitefrontend
    runtime: static
    buildCommand: bun install && bun run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
