# Frontend Environment Configuration
# Copy this file to .env.local and update with your actual values

# Backend API Configuration
# The base URL for your backend API endpoints
# Update this to match your backend server URL
VITE_API_BASE_URL=http://localhost:3001/api

# Development Note:
# When running locally, ensure your backend is running on the specified port.
# For production, update this to your deployed backend URL.

# NeonDB Configuration (Backend Reference)
# These variables are typically used in the backend, not frontend
# DATABASE_URL=postgresql://user:password@host/database
# DATABASE_AUTH_TOKEN=your_neon_auth_token
